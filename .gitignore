# Dependencies
node_modules/

# npm / package manager artefacts
# package-lock.json is intentionally committed for reproducible CI installs.
yarn.lock
pnpm-lock.yaml
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment & secrets
.env
.env.*
!.env.example

# Project-specific configuration generated by `init` (consumer-side, not committed in this repo)
spovishun-skills.config.yaml
spovishun-skills.config.yml

# Local Claude Code state (dogfooded self-install + session runtime).
# The artifact *source* lives in skills/ agents/ hooks/ rules/ — nothing under
# .claude/ is tracked in this repo, so ignore the whole directory.
.claude/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Build / test output
dist/
build/
coverage/
*.tgz
