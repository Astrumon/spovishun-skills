id: notion-workflow-spovishun
version: 1.0.2
category: stack-specific
requires:
  - notion
description: Project-level Notion workflow orchestrator. Routes operations to the correct skill, enforces REST-first hot path for task/board reads, and prevents duplicate MCP calls. Entry point for any Notion operation in a project that uses the full Spovishun Notion stack.
triggers:
  en:
    - "notion workflow"
    - "start notion operation"
  uk:
    - "notion операція"
    - "почни з Notion"
